version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "8000"
    environment:
      - DJANGO_SETTINGS_MODULE=resume_website.settings
    volumes:
      - static_volume:/usr/src/app/static
      - media_volume:/usr/src/app/media
    user: www-data
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: [ "CMD", "curl", "--fail", "http://localhost:8000/" ]
      interval: 30s
      timeout: 5s
      retries: 3

  db:
    image: postgres:latest
    environment:
      POSTGRES_DB: 'djangodb'
      POSTGRES_USER_FILE: /run/secrets/postgres_user
      POSTGRES_PASSWORD_FILE: /run/secrets/postgres_pass
    user: postgres
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $$(cat $${POSTGRES_USER_FILE})" ]
      interval: 30s
      timeout: 5s
      retries: 5
    secrets:
      - postgres_user
      - postgres_pass

  nginx:
    image: nginx:latest
    expose:
      - "80"
    volumes:
      - static_volume:/usr/src/app/static:ro
      - media_volume:/usr/src/app/media:ro
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro  # Adjust the path to your nginx configuration
    user: nginx
    depends_on:
      web:
        condition: service_healthy

  tunnel:
    container_name: cloudflared-tunnel
    image: cloudflare/cloudflared
    restart: unless-stopped
    command: tunnel run
    environment:
      - TUNNEL_TOKEN=mytokengoeshere
    user: cloudflared

secrets:
  postgres_user:
    file: ./secrets/postgres_user.txt
  postgres_pass:
    file: ./secrets/postgres_password.txt

volumes:
  static_volume:
  media_volume: